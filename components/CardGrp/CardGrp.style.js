import {StyleSheet} from "react-native";

export const s = StyleSheet.create({
    lineContainer : {
        width : '100%',
        height : 35,
        borderBottomWidth : 2,
        borderColor : 'black',
    },
    tabContainer : {
        borderWidth : 2,
        borderColor : 'black',
    }
})