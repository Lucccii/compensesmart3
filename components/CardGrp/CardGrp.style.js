import {StyleSheet} from "react-native";

export const s = StyleSheet.create({
    lineContainer : {
        width : '100%',
        height : 35,
        borderTopColor: 'black',
        borderTopWidth : 1.5,
    },
    tabContainer : {
        borderWidth : 2,
        borderColor : 'black',
        borderRadius : 5,
    }
})